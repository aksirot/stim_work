# FULL-SIZE Technique II on the merged inter-module circuit (solo retry, 2026-08-19).
# Attempt 1 OOM'd a 31GB box — running the r1 yaml's mw_workers:16 alongside two other
# DEM-heavy jobs (each BP-OSD worker holds the full 418k-column structure). This retry
# runs SOLO with mw_workers:3. Probe ladder so far: d_init=1 -> D=2, d_init=3 -> D=4
# (both = the d_init+1 time-boundary chain, PROVEN artifacts: the full circuit has
# zero weight-2 exposures by census). Production d_init=12 puts the boundary chain at
# ~13, so THIS measurement should finally see the merged code's SPATIAL distance.
# Target (arXiv:2506.03094 §2.4): deformed code distance d-1=11, onset 6.
label: gross_intermodule_r1_mw_full
outdir: runs/framework/bb144/inter_module_r1_mw_full
code_name: bb144
experiment: inter_module
decoder_name: relay
techniques: [II]

p_ref: 0.005
p_lo: 1.0e-4
p_hi: 0.005
p_coupler_factor: 1
lpu_C: 10
lpu_d_init: 12
lpu_idle_noise: true
lpu_include_memory_obs: false
ansatz_model: f5
pin_onset: false

relay_num_sets: 20
relay_stop_nconv: 5

mw_single_sector: false
mw_use_symmetry: false
mw_max_trials: 300
mw_workers: 3
mw_systematic: true

weights_range: [1, 1674]
adaptive: true
adaptive_failures: 20
adaptive_shots_min: 4
adaptive_shots_max: 3000
adaptive_stop_zero_bins: 3
