# Technique II MIDDLE probe for the merged inter-module circuit (2026-08-19).
# Scaling rung between the small probe (d_init=1, C=2 -> D=2, PROVEN a boundary
# artifact: the full 418k-mech circuit has ZERO weight-2 exposures by census) and
# the full-size search (OOMs 31GB; queued for fish). d_init=3, C=4 thickens the
# boundaries; watch whether D climbs toward the paper's deformed-code target
# (d-1=11, onset 6). D plateauing low WITH thick boundaries = real structural
# deficit (re-verify any candidate on the full circuit before believing it).
label: gross_intermodule_mw_probe_d6
outdir: runs/framework/bb144/inter_module_mw_probe_d6
code_name: bb144
experiment: inter_module
decoder_name: relay
techniques: [II]

p_ref: 0.005
p_lo: 1.0e-4
p_hi: 0.005
p_coupler_factor: 1
lpu_C: 6
lpu_d_init: 6
lpu_idle_noise: true
lpu_include_memory_obs: false
ansatz_model: f5
pin_onset: false

relay_num_sets: 20
relay_stop_nconv: 5

mw_single_sector: false
mw_use_symmetry: false
mw_max_trials: 400
mw_workers: 6
mw_systematic: true

weights_range: [1, 600]
adaptive: true
adaptive_failures: 20
adaptive_shots_min: 4
adaptive_shots_max: 3000
adaptive_stop_zero_bins: 3

